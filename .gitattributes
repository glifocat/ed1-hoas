# Tell GitHub Linguist to count YAML as code (not data)
# so the repo language reflects ESPHome configurations
*.yaml linguist-detectable
*.yml linguist-detectable

# Normalize line endings: yamllint (CI) requires Unix newlines
*.yaml text eol=lf
*.yml text eol=lf
