# ==============================================================================
# ED1 IR Receiver Package - 38kHz infrared remote control
# ==============================================================================

remote_receiver:
  id: ir_receiver
  pin:
    number: GPIO35
    inverted: true
  dump: all
  tolerance: 25%
  idle: 10ms
  buffer_size: 10000b
